from django.db import models
from order.models import Order

# Create your models here.
# class CafePayments(models.Model):
#     order_number = models.OneToOneField(Order.id)
#     total_amount = models.OneToOneField(Order)